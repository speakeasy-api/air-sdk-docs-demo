
import WithStatusBar from '@/src/components/WithStatusBar';

<WithStatusBar statusItems={[["Status", "200"], ["Content-Type", "application/json"]]}>
  <CH.Code>
  ```python InitiateCall.py
  import air
from air.models import components

s = air.Air(
    bearer_auth="",
)

req = components.Call(
    metadata=components.Metadata(),
    phone='678-685-8466 x3428',
    prompt_id=56713,
)

res = s.call.initiate_call(req)

if res.object is not None:
    # handle response
    pass
  ```
  ---

  ```json Example Response
  {
    "status": "string"
  }
  ```
  </CH.Code>
</WithStatusBar>
