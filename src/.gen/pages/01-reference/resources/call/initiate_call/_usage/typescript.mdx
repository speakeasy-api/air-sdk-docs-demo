
import WithStatusBar from '@/src/components/WithStatusBar';

<WithStatusBar statusItems={[["Status", "200"], ["Content-Type", "application/json"]]}>
  <CH.Code>
  ```typescript InitiateCall.ts
  import { Air } from "@air-ai/air-sdk-typescript";

(async() => {
  const sdk = new Air({
    bearerAuth: "",
  });

  const res = await sdk.call.initiateCall({
    metadata: {},
    phone: "678-685-8466 x3428",
    promptId: 56713,
  });

  if (res.statusCode == 200) {
    // handle response
  }
})();
  ```
  ---

  ```json Example Response
  {
    "status": "string"
  }
  ```
  </CH.Code>
</WithStatusBar>
