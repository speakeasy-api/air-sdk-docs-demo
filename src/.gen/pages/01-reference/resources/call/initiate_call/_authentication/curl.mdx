import Collapsible from "/src/components/Collapsible";
import Labels from "/src/lib/labels";

##### HTTP Bearer _— in HTTP header_
  
Set your bearer token in a `Authorization` HTTP header.

Example: `Authorization: `
  
