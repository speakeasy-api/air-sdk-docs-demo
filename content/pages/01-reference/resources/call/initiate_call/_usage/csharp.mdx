
import WithStatusBar from '@/src/components/WithStatusBar';

<WithStatusBar statusItems={[["Status", "200"], ["Content-Type", "application/json"]]}>
  <CH.Code>
  ```csharp InitiateCall.cs
  using Air;
using Air.Models.Components;

var sdk = new Air(
    security: new Security() {
        BearerAuth = "",
    }
);

var res = await sdk.Call.InitiateCallAsync(new Call() {
    Metadata = new Metadata() {},
    Phone = "678-685-8466 x3428",
    PromptId = 56713,
});

// handle response
  ```
  ---

  ```json Example Response
  {
    "status": "string"
  }
  ```
  </CH.Code>
</WithStatusBar>
